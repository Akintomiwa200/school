# Copy to `.env.local` and set your machine's LAN IP so a physical device can reach the API.
# Find IP: Windows `ipconfig`, macOS/Linux `ifconfig` or `ip addr`
#
# EXPO_PUBLIC_API_URL=http://192.168.1.42:3000
